# 🔧 Решение ошибок при запуске бота

## ❌ Ошибка: `TelegramUnauthorizedError: Unauthorized`

Эта ошибка означает, что **BOT_TOKEN невалиден, истёк или скопирован неправильно**.

### 🔍 Причины

1. **BOT_TOKEN неверный или истёк**
2. **Скопирован с пробелами или кавычками**
3. **BOT_TOKEN не загружается из .env**
4. **Токен удалён или переревокирован в @BotFather**

---

## ✅ Решение

### Шаг 1: Получите новый токен

1. **Откройте Telegram**
2. **Найдите бота:** `@BotFather`
3. **Отправьте:** `/mybots`
4. **Выберите свой бот → API Token**
5. **Скопируйте полный токен** (вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Проверьте формат копирования

**Неправильно ❌**
```env
BOT_TOKEN = 123456789:ABCdef...     # лишние пробелы
BOT_TOKEN='123456789:ABCdef...'     # кавычки
BOT_TOKEN="123456789:ABCdef..."    # кавычки
```

**Правильно ✓**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### Шаг 3: Обновите `.env`

#### На локальной машине
```bash
# Отредактируйте .env в корне проекта
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=your_id

# Сохраните и запустите
python main.py
```

#### На BotHost

**Вариант 1: Через `.env` файл (рекомендуется)**
1. Обновите `.env` локально
2. Сделайте git commit и push
3. В панели BotHost нажмите **Restart**

**Вариант 2: Через Environment Variables**
1. Перейдите в **Settings → Environment Variables**
2. Найдите или создайте переменную `BOT_TOKEN`
3. Вставьте новый токен (без пробелов и кавычек)
4. Нажмите **Save**
5. Нажмите **Restart**

---

## 📋 Проверочный список

- [ ] Токен скопирован полностью (вместе с `123456789:` частью)
- [ ] Нет пробелов в начале или конце
- [ ] Нет кавычек (одинарных или двойных)
- [ ] Файл `.env` находится в корне проекта (где `main.py`)
- [ ] Формат: `BOT_TOKEN=token_here` (без пробелов вокруг `=`)
- [ ] Файл сохранён в UTF-8
- [ ] После изменения `.env` перезапустили приложение

---

## 🔄 Если после перезапуска всё ещё не работает

### 1. Проверьте логи при запуске

В логах должно быть:
```
📄 Loading .env from project root: /app/.env
✓ BOT_TOKEN loaded (starts with: 123456789:AB...)
```

Если этого нет → `.env` не загружается.

### 2. Убедитесь, что `.env` существует

```bash
# На BotHost через консоль
cat /app/.env

# Должны увидеть
BOT_TOKEN=123456789:ABCdef...
```

### 3. Проверьте права доступа на BotHost

```bash
ls -la .env
# Должно быть что-то вроде:
# -rw-r--r--  1 root root  123 Jan 28 20:00 .env
```

### 4. Попробуйте через Environment Variables вместо .env

Иногда на хостинге `.env` файлы не работают, используйте вместо этого:
- Settings → Environment Variables в панели BotHost
- Добавьте там `BOT_TOKEN` прямо

---

## 💡 Подсказки

### Как получить и проверить ID токена

```python
# Быстрая проверка формата токена
token = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"

# Проверим части
parts = token.split(":")
print(f"Bot ID: {parts[0]}")
print(f"Secret: {parts[1][:10]}...")
# Output:
# Bot ID: 123456789
# Secret: ABCdefGHIj...
```

### Если токен был переревокирован

Если вы потеряли токен или он был скомпрометирован:
1. В `@BotFather` → /mybots → выберите бот
2. **Edit Bot → API Token → Generate new token**
3. Скопируйте новый токен
4. Обновите `.env` или Environment Variables
5. Перезапустите

---

## 🚀 После исправления

После обновления токена в логах должны быть сообщения вида:

```
✓ BOT_TOKEN is valid (123456789:AB...)
✓ Telegram bot initialized successfully
```

И бот должен **начать polling** (опрашивать сообщения).

---

**Остались вопросы?** Проверьте полное руководство в `ADMIN_CONFIG.md`
